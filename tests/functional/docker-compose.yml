version: '3.7'

services:

  movie_test:
    container_name: movie_test
    image: async_api_sprint_1_movie_api
    build:
      context: .
    entrypoint: >
      sh -c "pytest src/"
    volumes:
      - .:/usr/src/tests/
